image:
  repository: nestorx211/api-microservice
  tag: dev
  pullPolicy: Always

env: dev

replicaCount: 1

resources:
  requests:
    cpu: 200m
    memory: 500Mi
  limits:
    cpu: 600m
    memory: 1Gi

hpa:
  minReplicas: 1
  maxReplicas: 4
  cpuUtilization: 70

ingress:
  path: /users(/|$)(.*)
