apiVersion: v1
kind: Service
metadata:
  name: api-microservice
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: api-microservice
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
